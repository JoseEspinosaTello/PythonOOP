item1 = 'phone'
item1_price = 100
item_quantity = 5
item1_price_total = item1_price * item_quantity


print(type(item1)) #str
print(type(item1_price)) #int
print(type(item_quantity)) #int
print(type(item1_price_total)) #int

#when run for each of the types we also see the keyword class
#this means that these datatypes are actually instances of strings or integers
#in python each datatype is an object that has been instatiated earlier by a class